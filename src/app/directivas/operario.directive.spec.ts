import { OperarioDirective } from './operario.directive';

describe('OperarioDirective', () => {
  it('should create an instance', () => {
    const directive = new OperarioDirective();
    expect(directive).toBeTruthy();
  });
});
