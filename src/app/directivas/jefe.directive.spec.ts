import { JefeDirective } from './jefe.directive';

describe('JefeDirective', () => {
  it('should create an instance', () => {
    const directive = new JefeDirective();
    expect(directive).toBeTruthy();
  });
});
